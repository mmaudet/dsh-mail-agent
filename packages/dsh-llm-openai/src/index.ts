/**
 * OpenAI-compatible LLM adapter and four-tier model router for the DSH mail agent
 *
 * Phase 0 scaffold: this bundle intentionally ships no runtime behaviour yet.
 */

export const BUNDLE_ID = '@dsh-mail-agent/llm-openai' as const;

export type BundleId = typeof BUNDLE_ID;
