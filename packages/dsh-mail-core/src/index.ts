/**
 * Mail contract, adapters, cascade loop and tools for the DSH mail agent
 *
 * Phase 0 scaffold: this bundle intentionally ships no runtime behaviour yet.
 */

export const BUNDLE_ID = '@dsh-mail-agent/mail-core' as const;

export type BundleId = typeof BUNDLE_ID;
